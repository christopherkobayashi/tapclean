
"cd src"
"make" (on MAC OSX use "OS=MACOSX make") 
Copy tapclean or tapclean.exe to a suitable directory (it requires write access)
Make sure the directory is in your system PATH if you want to execute tapclean from any location
